{
  "name": "@dominicbarnes/eslint-config-test",
  "description": "ESLint configuration for use in project tests",
  "version": "0.1.1",
  "main": "index.json",
  "peerDependencies": {
    "eslint": "^2.0.0",
    "eslint-plugin-mocha": "^2.0.0"
  },
  "devDependencies": {
    "jsonlint": "^1.6.2"
  },
  "scripts": {
    "lint": "jsonlint -q index.json",
    "test": "npm run lint"
  }
}
